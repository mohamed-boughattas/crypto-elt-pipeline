crypto_elt_pipeline:
  target: dev
  outputs:
    dev:
      type: duckdb
      # Resolves the database file relative to the dbt project root
      path: '../data/crypto.duckdb'
      # Configures parallel execution for concurrent model materialization
      threads: 4
